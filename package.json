{
    "name": "mermaid-chart-block",
    "version": "1.0.0",
    "description": "A production-ready Gutenberg block using PNPM, Webpack, and mmc for local diagram rendering.",
    "license": "GPL-2.0-or-later",
    "scripts": {
        "build": "wp-scripts build",
        "start": "wp-scripts start",
        "format": "wp-scripts format",
        "lint:js": "wp-scripts lint-js",
        "lint:style": "wp-scripts lint-style",
        "deploy:local": "wp-scripts build && zip -r mermaid-chart-block.zip . -x '*.git*' -x 'node_modules/*'"
    },
    "devDependencies": {
        "@wordpress/scripts": "^26.0.0",
        "sass": "^1.66.1"
    },
    "dependencies": {
        "mermaid": "*"
    }
}